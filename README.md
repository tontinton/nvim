## Install

```bash
# Backup
mv ~/.config/nvim ~/.config/nvim.bak

# Clear cache
rm -rf ~/.local/share/nvim

# Install
git clone https://github.com/tontinton/nvim ~/.config/nvim --depth 1 && nvim
```
